/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FillPipe } from './fill.pipe';

describe('Pipe: Fill', () => {
  it('create an instance', () => {
    let pipe = new FillPipe();
    expect(pipe).toBeTruthy();
  });
});
